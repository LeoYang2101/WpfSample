<Page x:Class="WpfSample.WpfSqlite.Views.PageAllTask"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfSample.WpfSqlite.Views"
      xmlns:hc="https://handyorg.github.io/handycontrol" xmlns:viewmodel="clr-namespace:WpfSample.WpfSqlite.ViewModel"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="PageAllTask">
    <Page.DataContext>
        <viewmodel:PageAllTaskViewModel/>
    </Page.DataContext>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Add new item -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBox Text="{Binding NewTitle, UpdateSourceTrigger=PropertyChanged}" 
                 Width="200" Margin="0,0,5,0" />
            <TextBox Text="{Binding NewDescription, UpdateSourceTrigger=PropertyChanged}" 
                 Width="200" Margin="0,0,5,0"/>
            <Button Content="Add" Command="{Binding AddCommand}" Width="60"/>
        </StackPanel>

        <!-- Action buttons -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="Delete" Command="{Binding DeleteCommand}" Width="60" Margin="0,0,5,0"/>
            <Button Content="Toggle Complete" Command="{Binding ToggleCompleteCommand}"/>
        </StackPanel>

        <!-- Todo items list -->
        <ListView Grid.Row="2" ItemsSource="{Binding TodoItems}" SelectedItem="{Binding SelectedItem}" BorderThickness="0">          
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Completed" Width="80">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <CheckBox IsChecked="{Binding IsCompleted,UpdateSourceTrigger=PropertyChanged}" IsEnabled="True"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="Title" DisplayMemberBinding="{Binding Title}" Width="150" TextBlock.FontStyle="Oblique"/>
                    <GridViewColumn Header="Description" DisplayMemberBinding="{Binding Description}" Width="250"/>
                    <GridViewColumn Header="Created" DisplayMemberBinding="{Binding CreatedDate, StringFormat=yyyy-MM-dd}" Width="100"/>
                    <GridViewColumn Header="Due Date" DisplayMemberBinding="{Binding DueDate, StringFormat=yyyy-MM-dd}" Width="100"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Page>
