<Page x:Class="WpfSample.WpfSqlite.Views.PageTaskManagement"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfSample.WpfSqlite.ViewModel" 
      xmlns:resource="clr-namespace:WpfSample.WpfSqlite.Resource"
      xmlns:sys="clr-namespace:System;assembly=mscorlib"
      xmlns:scm="clr-namespace:System.ComponentModel;assembly=WindowsBase"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="PageTaskManagement">
    <Page.Resources>
        <resource:PriorityToTextConverter x:Key="PriorityToTextConverter"/>
        <resource:StatusToTextConverter x:Key="StatusToTextConverter"/>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </Page.Resources>
    <Page.DataContext>
        <local:PageTaskManagementViewModel />
    </Page.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 工具栏 -->
        <ToolBar Grid.Row="0">
            <Button Content="新增" Command="{Binding AddCommand}"/>
            <Button Content="编辑" Command="{Binding EditCommand}"/>
            <Button Content="删除" Command="{Binding DeleteCommand}"/>
            <Separator/>
            <Button Content="保存" Command="{Binding SaveCommand}"/>
            <Button Content="取消" Command="{Binding CancelCommand}"/>
        </ToolBar>

        <!-- 任务列表 -->
        <DataGrid Grid.Row="1" 
                  ItemsSource="{Binding Tasks}" 
                  SelectedItem="{Binding SelectedTask}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="标题" Binding="{Binding Title}" Width="*"/>
                <DataGridTextColumn Header="分类" Binding="{Binding Category.Name}" Width="100"/>
                <DataGridTextColumn Header="优先级" Binding="{Binding Priority}" Width="80">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="Text" Value="{Binding Path=Priority, Converter={StaticResource PriorityToTextConverter}}"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="截止日期" Binding="{Binding DueDate, StringFormat=yyyy-MM-dd}" Width="120"/>
                <DataGridTextColumn Header="状态" Binding="{Binding Status}" Width="80">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="Text" Value="{Binding Path=Status, Converter={StaticResource StatusToTextConverter}}"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- 详情编辑区 -->
        <GridSplitter Grid.Row="1" HorizontalAlignment="Stretch" VerticalAlignment="Bottom" 
                      Height="5" Background="Gray" ShowsPreview="True"/>

        <Grid Grid.Row="2" DataContext="{Binding SelectedTask}" Visibility="{Binding IsTaskSelected, Converter={StaticResource BooleanToVisibilityConverter}}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Label Grid.Row="0" Grid.Column="0" Content="标题:"/>
            <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>

            <Label Grid.Row="1" Grid.Column="0" Content="分类:"/>
            <ComboBox Grid.Row="1" Grid.Column="1" 
                      ItemsSource="{Binding DataContext.Categories, RelativeSource={RelativeSource AncestorType=Window}}"
                      DisplayMemberPath="Name"
                      SelectedValuePath="CategoryId"
                      SelectedValue="{Binding CategoryId}"
                      Margin="5"/>

            <Label Grid.Row="2" Grid.Column="0" Content="优先级:"/>
            <ComboBox Grid.Row="2" Grid.Column="1" 
                      SelectedIndex="{Binding Priority}"
                      Margin="5">
                <ComboBoxItem Content="高"/>
                <ComboBoxItem Content="中"/>
                <ComboBoxItem Content="低"/>
            </ComboBox>

            <Label Grid.Row="3" Grid.Column="0" Content="截止日期:"/>
            <DatePicker Grid.Row="3" Grid.Column="1" SelectedDate="{Binding DueDate}" Margin="5"/>

            <Label Grid.Row="4" Grid.Column="0" Content="描述:"/>
            <TextBox Grid.Row="4" Grid.Column="1" Text="{Binding Description}" 
                     AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"
                     Margin="5" Height="100"/>
        </Grid>
    </Grid>
</Page>
